# Cloudflare Pages configuration
# This file is automatically detected by Cloudflare Pages

# Build configuration
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"

# Deploy command (for manual deployment)
# Use: npx wrangler pages deploy out --project-name=x-blog
